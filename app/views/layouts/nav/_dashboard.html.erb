<nav class="navbar sticky-top navbar-expand-lg " style="background-color: #a1cfe7;">
  <a class="navbar-brand" style="text-decoration:none !important;"href="#"> House4U</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav ">
      <li class="nav-item active">
        <%=link_to "Home" ,root_path,class:"nav-link", style: "text-decoration: none;"%><span class="sr-only">(current)</span>
      </li>
        
      <li class="nav-item" style="margin-top:16px;font-size:1.4em">
      <%if user_signed_in?%>
        <%= link_to "show property",  root_path, style: "text-decoration: none;"%>
        <%end%>
      </li>
      <li class="nav-item" style="margin-top:16px;font-size:1.4em">
        <%if agent_signed_in?%>
        <%= link_to "List of property" ,properties_path , style: "text-decoration: none;"%>
        <%= link_to 'Create property',new_property_path, style: "text-decoration: none;" %>
         <!-- <%= link_to 'Show Comments',agents_path(current_agent.id) %> -->
        <%end%>
      </li>
      <% if agent_signed_in? %>
        <li class="nav-item text-nowrap">
          <%= button_to "sign Out" ,destroy_agent_session_path, method: :delete,class:"btn btn-lg btn-block", style:"margin-top: 19px;"%>
        </li>
        <% elsif user_signed_in?  %>
        <li class="nav-item" >
        <%= button_to "sign Out" ,destroy_user_session_path, method: :delete,class:"btn btn-lg btn-block ",style:"margin-top: -6px;" %>
      </li>
      <%end%>
    </ul>
    
  </div>
</nav>

















































